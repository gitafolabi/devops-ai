apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: azure-kv
spec:
  provider:
    azurekv:
      vaultUrl: "https://crud-kv.vault.azure.net/"
      authType: ManagedIdentity
      identityId: "<KUBELET_CLIENT_ID>"
